def reformat_languages(languages)
  # your code here
  new_hash={}
 languages.each do |styles|
styles.each do |languages|
languages.each do |type,style|
new_hash[:languages]={
  type="#{:type}",
  style="#{:style}"
}
  return new_hash
end

def reformat_languages(languages)
  # your code here
  new_hash = {}
  languages.each do |mode, mode_data|
    style_data.each do |lang, type|
      new_hash[lang] = {:style:
      [mode]}
    end
  end
  new_hash
end
